import { TrackerLinkPipe } from './tracker-link.pipe';

describe('TrackerLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackerLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
