import { SessionDistancePipe } from './session-distance.pipe';

describe('SessionDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new SessionDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
