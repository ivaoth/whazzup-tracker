import { MultipleSessionDistancePipe } from './multiple-session-distance.pipe';

describe('MultipleSessionDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleSessionDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
